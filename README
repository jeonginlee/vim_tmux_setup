Set up for Vim:

Windows:
	Set environment variable $VIM to this directory

Set up for tmux:

Place the file tmux.conf in either ~/ for user settings, or /etc/ for global
settings

Add to your bash.rc: TERM=screen-256color [[ -z "$TMUX" ]]

Leader mapped to C-a
